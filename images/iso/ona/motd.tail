#!/bin/sh

cat << EOF

This machine is an Observable Networks Sensor.

It should have at least two network interfaces:
* The default interface should have Internet access for management traffic
* The other(s) should be connected to mirror / SPAN ports on a switch / router

Use the web portal (<your-org>.obsrvbl.com) to manage this sensor.
EOF
