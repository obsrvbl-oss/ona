#!/bin/sh

cat << EOF

This machine is a Stealthwatch Cloud sensor.

Use the web portal at https://<your-org>.obsrvbl.com to manage this sensor.

To make local configuration changes, edit the file:
/opt/obsrvbl-ona/config.local

To test connectivity to Stealthwatch Cloud, run this command:
curl https://sensor.ext.obsrvbl.com

EOF
